/*
    A subset is a set of which all the elements are contained in another set
    Given an array, return the array contain all unique subsets
    Example: sort([a, b, c]) should return ([[a], [a, b], [a, b, c], [b], [b, c], [c]]);
*/

function subsets(array) {
    
}